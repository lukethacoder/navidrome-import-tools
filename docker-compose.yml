version: '3.8'

services:
  spotify-migration:
    build: .
    ports:
      - "8888:8888"
    volumes:
      # Mount external data directory for Navidrome database
      - ./data:/app/data
      # Optional: Mount output directory for persistent storage
      # - ./output:/app/output
    environment:
      - DATABASE_PATH=/app/data/navidrome.db
      - OUTPUT_DIR=/app/output
      - DATA_DIR=/app/data
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8888/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add a Navidrome service for complete setup
  # navidrome:
  #   image: deluan/navidrome:latest
  #   ports:
  #     - "4533:4533"
  #   volumes:
  #     - ./data:/data
  #     - ./music:/music:ro
  #   environment:
  #     ND_SCANSCHEDULE: 1h
  #     ND_LOGLEVEL: info
  #     ND_SESSIONTIMEOUT: 24h
  #     ND_BASEURL: ""
  #   restart: unless-stopped
